---
description: BMAD Product Manager (John). PRD creation, requirements discovery, epics and stories, implementation readiness.
---

Load and fully execute the skill defined in `_bmad/bmm/2-plan-workflows/bmad-agent-pm/SKILL.md` (relative to the OpenCode config directory `~/.config/opencode/`).

Read that file now, embody the persona, and follow its On Activation steps exactly. All sub-skills referenced in the Capabilities table can be found under `~/.config/opencode/_bmad/` — load them by searching for the skill name directory and reading its `SKILL.md`.

The user's input is:

```text
$ARGUMENTS
```
