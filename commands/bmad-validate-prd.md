---
description: Validate a PRD against BMAD standards through comprehensive review.
---

Load and fully execute the skill defined in `_bmad/bmm/2-plan-workflows/bmad-validate-prd/SKILL.md` (relative to the OpenCode config directory `~/.config/opencode/`).

Read that file now and follow its instructions exactly. All sub-skills and step files referenced can be found under `~/.config/opencode/_bmad/` — load them by searching for the skill name directory and reading its files.

The user's input is:

```text
$ARGUMENTS
```
