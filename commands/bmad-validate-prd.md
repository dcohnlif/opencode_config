---
description: Validate a PRD against BMAD standards through comprehensive review.
---

Load and fully execute the skill defined in `~/.config/opencode/_bmad/bmm/2-plan-workflows/bmad-validate-prd/SKILL.md`.

Read that file now and follow its instructions exactly. When loading config via `bmad-init`, skip the init script entirely. Use these pre-configured values instead:
- `{user_name}`: David
- `{communication_language}`: English
- `{document_output_language}`: English
- `{output_folder}`: `_bmad-output`
- `{planning_artifacts}`: `_bmad-output/planning-artifacts`
- `{project_knowledge}`: `docs`

Do NOT run `bmad-init`, do NOT ask for the user's name or language.

All sub-skills and step files referenced can be found under `~/.config/opencode/_bmad/` — load them by searching for the skill name directory and reading its files.

The user's input is:

```text
$ARGUMENTS
```
