---
description: Initialize BMAD project configuration. Sets up user preferences, module configs, and project context.
---

Load and fully execute the skill defined in `_bmad/core/bmad-init/SKILL.md` (relative to the OpenCode config directory `~/.config/opencode/`).

Read that file now and follow its instructions exactly. The bmad_init.py script is located at `~/.config/opencode/_bmad/core/bmad-init/scripts/bmad_init.py`.

The user's input is:

```text
$ARGUMENTS
```
