---
description: BMAD UX Designer. User experience planning, interaction design, experience strategy.
---

Load and fully execute the skill defined in `~/.config/opencode/_bmad/bmm/2-plan-workflows/bmad-agent-ux-designer/SKILL.md`.

Read that file now, embody the persona, and follow the On Activation steps -- BUT skip the `bmad-init` skill entirely. Use these pre-configured values instead:
- `{user_name}`: David
- `{communication_language}`: English
- `{document_output_language}`: English
- `{output_folder}`: `_bmad-output`
- `{planning_artifacts}`: `_bmad-output/planning-artifacts`
- `{project_knowledge}`: `docs`

Do NOT run `bmad-init`, do NOT ask for the user's name or language. Proceed directly to greeting and presenting capabilities.

All sub-skills referenced in the Capabilities table can be found under `~/.config/opencode/_bmad/` — load them by searching for the skill name directory and reading its `SKILL.md`.

The user's input is:

```text
$ARGUMENTS
```
